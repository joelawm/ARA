[package]
name = "axum"
version = "0.1.0"
authors = ["Joe Meyer"]
edition = "2021"

[dependencies]
axum = "0.8.1"
tokio = { version = "1.38.0", features = ["full"] }

#visual-lib = { path = "../visual-lib", features = ["cors", "logging"]}